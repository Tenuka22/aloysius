{
  "name": "@aloysius-web/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development alchemy dev --profile='default'",
    "deploy": "NODE_ENV=production alchemy deploy --profile='default' --stage=prod",
    "destroy": "NODE_ENV=production alchemy destroy --profile='default' --stage=prod"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@aloysius-web/config": "workspace:*",
    "alchemy": "catalog:",
    "typescript": "catalog:"
  }
}
