{
  "name": "@web-template/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development alchemy dev --profile='template'",
    "deploy": "NODE_ENV=production alchemy deploy --profile='template' --stage=prod",
    "destroy": "NODE_ENV=production alchemy destroy --profile='template' --stage=prod"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@web-template/config": "workspace:*",
    "alchemy": "catalog:",
    "typescript": "catalog:"
  }
}
